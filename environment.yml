name: xvg-analysis
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - numpy>=1.24.0
  - pandas>=2.0.0
  - plotly>=5.14.0
  # Development dependencies (uncomment to install)
  # - black>=23.0.0
  # - flake8>=6.0.0
  # - mypy>=1.4.0
  # - pytest>=7.0.0
  # - python-build>=1.0.0
  - pip:
    - -e .
